# Antigravity Agents Guidelines for Notion-TUI

## 1. プロジェクトの概要と目的
本プロジェクトは、Notionのデータベースをバックエンドとして利用し、Terminal UI (TUI) 経由で爆速・直感的にデータの閲覧・編集を行うためのCLIツール（`ntui`）です。現在、買い物・在庫管理（`shop`）とモンスター育成管理（`monst`）の機能を備えています。

## 2. ディレクトリ構成とアーキテクチャ
本リポジトリは、以下の役割分担に基づいてクリーンに分離されています。
* `cmd/ntui/`: アプリケーションのエントリーポイントおよび Cobra を用いたサブコマンドのルーティング。
* `internal/domain/`: アプリケーション全体のドメインモデル（構造体定義）。他パッケージに依存しないプレーンなGoの型を配置。
* `internal/notion/`: Notion API との通信を担当。`github.com/jomei/notionapi` パッケージをラップし、TUIモデルが扱いやすい形にパースして返す。
* `internal/tui/`: `github.com/charmbracelet/bubbletea` を用いた TUI アプリケーションの実装（Model, Update, View のライフサイクル）。

## 3. 実装上の重要なルールと設計思想
* **インメモリキャッシュと高速化**:
  TUIの最大の強みである「軽快な操作感」を損なわないため、リストのフィルタリングやソート（例: 属性ごとの絞り込み、カテゴリー切替など）は**APIリクエストを都度発行するのではなく、初回起動時に全データをキャッシュし、インメモリで処理**することを基本方針とします。
* **動的抽出の推奨**:
  カテゴリーやイベント名といった「分類」は、マスタデータ（Notion側）から取得したデータ全体を走査し、ユニークなリストをインメモリで自動抽出・生成することで、将来的なデータの追加にもコード改修なしで対応できるようにします。
* **Notion APIの仕様に関する注意点**:
  * NotionAPI経由でのリレーションプロパティの更新時、`RelationObject` ではなく単なる `Relation` 構造体 (`Relation{ID: PageID(id)}`) を使用しないと `400 Bad Request` が返る仕様があるため、更新処理の実装時には既存のクライアント実装を参考にすること。
* **UI/UXの操作統一**:
  ユーザーの直感的な操作を担保するため、新しい機能や画面を追加する際も以下のキーバインド規約に準じて設計してください。
  * `q` / `ctrl+c`: アプリの完全終了
  * `esc`: 一つ前の状態に戻る、またはキャンセル
  * `tab` / `left` / `right`: 複数ペインが存在する場合のフォーカス移動
  * `/` または `f`: フィルター・ソート・検索モードの呼び出し
  * `s`: 編集内容の保存（Notion APIへの送信）
* **テストの制約とモックの活用**:
  Bubble Tea の `Update` ロジック等に複雑な状態遷移が追加された場合はテストを記述してください。APIクライアントのインターフェースをモック化して検証を行いますが、状態（スライスやマップ）の初期化を怠ると容易に `panic` するため、テスト時のダミーデータ構築は網羅的に行うこと。

## 4. 実行・ビルド方法
* 実行: `go run ./cmd/ntui [subcommand]` (例: `go run ./cmd/ntui shop`)
* テスト: `go test ./...`
